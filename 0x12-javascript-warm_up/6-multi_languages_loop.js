#!/usr/bin/node
//A script that prints 3 lines, but by using an array of string and a loop

	const myLines = ["C is fun", "Python is cool", "JavaScript is amazing"];
	const arrayLength = myLines.length;
	let n = 0;
	while (i < arrayLength) {
	  console.log(myLines[n]);
	  n++;
	}
